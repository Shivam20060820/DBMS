Windows PowerShell
Copyright (C) Microsoft Corporation. All rights reserved.

Install the latest PowerShell for new features and improvements! https://aka.ms/PSWindows

Loading personal and system profiles took 1104ms.
PS C:\WINDOWS\system32> mysql -u root -p
Enter password: ****************
Welcome to the MySQL monitor.  Commands end with ; or \g.
Your MySQL connection id is 8
Server version: 8.0.45 MySQL Community Server - GPL

Copyright (c) 2000, 2026, Oracle and/or its affiliates.

Oracle is a registered trademark of Oracle Corporation and/or its
affiliates. Other names may be trademarks of their respective
owners.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

mysql> use dbms_project1
Database changed
mysql> DELIMITER ;;
mysql> CREATE FUNCTION GetCaseDuration(fid INT)
    -> RETURNS INT
    -> DETERMINISTIC
    -> BEGIN
    ->     DECLARE days INT;
    ->
    ->     SELECT DATEDIFF(
    ->         IFNULL(judgement_date, CURDATE()),
    ->         filing_date
    ->     )
    ->     INTO days
    ->     FROM case_details
    ->     WHERE fir_id = fid;
    ->
    ->     RETURN days;
    -> END;;
Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;
mysql> drop function GetCaseDuration
    -> ;
Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;;
mysql> CREATE FUNCTION GetCaseDuration(fid INT)
    -> RETURNS INT
    -> DETERMINISTIC
    -> BEGIN
    ->     DECLARE days INT;
    ->
    ->     SELECT DATEDIFF(
    ->         IFNULL(judgement_date, CURDATE()),
    ->         filing_date
    ->     )
    ->     INTO days
    ->     FROM case_details
    ->     WHERE fir_id = fid;
    ->
    ->     RETURN days;
    -> END;;
Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;
mysql> DELIMITER ;;
mysql> CREATE FUNCTION IsRepeatOffender(cid INT)
    -> RETURNS VARCHAR(10)
    -> DETERMINISTIC
    -> BEGIN
    ->     DECLARE total INT;
    ->
    ->     SELECT COUNT(*) INTO total
    ->     FROM fir
    ->     WHERE cri_id = cid;
    ->
    ->     IF total > 1 THEN
    ->         RETURN 'YES';
    ->     ELSE
    ->         RETURN 'NO';
    ->     END IF;
    -> END;;
Query OK, 0 rows affected (0.00 sec)

mysql> DELIMITER ;
mysql> DELIMITER ;;
mysql> CREATE PROCEDURE GetActiveCases()
    -> BEGIN
    ->     SELECT * FROM case_details
    ->     WHERE case_status = 'Ongoing';
    -> END;;
Query OK, 0 rows affected (0.00 sec)

mysql> DELIMITER ;
mysql> CREATE VIEW CaseOverview AS
    -> SELECT
    ->     f.fir_no,
    ->     c.case_status,
    ->     c.judgement,
    ->     cr.name AS criminal_name,
    ->     v.name AS victim_name
    -> FROM fir f
    -> JOIN case_details c ON f.fir_id = c.fir_id
    -> LEFT JOIN criminal cr ON f.cri_id = cr.cri_id
    -> LEFT JOIN victim v ON f.fir_id = v.fir_id;
Query OK, 0 rows affected (0.02 sec)

mysql> CREATE VIEW ActiveCases AS
    -> SELECT *
    -> FROM case_details
    -> WHERE case_status = 'Ongoing';
Query OK, 0 rows affected (0.01 sec)

mysql> drop procedure GetActiveCases
    -> ;
Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;;
mysql> CREATE TRIGGER update_criminal_status
    -> AFTER INSERT ON jail_records
    -> FOR EACH ROW
    -> BEGIN
    ->     UPDATE criminal
    ->     SET status = 'In Jail'
    ->     WHERE cri_id = NEW.cri_id;
    -> END;;
Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;
